$('.formCard').remove();
$('#new_link').show();
location.reload();


// code below reload the page which will at the same timeorganize it alphabetically

//$('tbody').append('<%= j render(@employee) %>');
//alert("Employee Created")
//$('#employeesAJAX').append('<%= j render(@employee) %>');

// Code below could be used if I want to add the employee to mark the new added employee to the list
// But it will be removed with the reload of the page so another strategy is needed.



//location.reload()
//$('.newadded').append('<%= j render(@employee) %>');
//newadded

